'use strict';

/**
 * @param {object[]} messages - Conversation history { role, content }[]
 * @returns {string} System + user prompt for intent extraction
 */
function extractIntentPrompt(messages) {
  throw new Error('Not implemented');
}

module.exports = { extractIntentPrompt };
