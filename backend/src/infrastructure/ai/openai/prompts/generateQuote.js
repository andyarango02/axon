'use strict';

/**
 * @param {object} requirements - Structured requirements extracted from conversation
 * @param {object[]} products - Matching products from the catalog
 * @returns {string} Prompt that instructs the model to generate a quotation draft as JSON
 */
function generateQuotePrompt(requirements, products) {
  throw new Error('Not implemented');
}

module.exports = { generateQuotePrompt };
