'use strict';

const ProductType = Object.freeze({
  PHYSICAL: 'PHYSICAL', // tangible product with stock
  SERVICE:  'SERVICE',  // intangible — may have no fixed base price
});

module.exports = ProductType;
